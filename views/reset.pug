extends layout

block content
	.inner
		form.form(method="POST")
			h2 Nullstill passordet
			label(for="password") Passord
			input(type="password" name="password")
			label(for="password-confirm") Bekreft passord
			input(type="password" name="password-confirm")
			input.button(type="submit" value="Oppdater passord")